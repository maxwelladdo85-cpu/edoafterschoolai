import { Y as reactExports, P as jsxRuntimeExports } from "./server-Cxw6WwHr.js";
import { u as useAuth, h as useNavigate, L as Link } from "./router-BcETnmHN.js";
import { s as supabase } from "./client-Ba9waXZY.js";
import { b as Award, c as ClipboardCheck, S as SidebarProvider, a as AppSidebar, d as SidebarTrigger, N as NotificationBell, V as Video } from "./NotificationBell-BKNCo5D8.js";
import { C as Card, c as CardHeader, d as CardTitle, a as CardContent } from "./card-h2noaq3f.js";
import { B as Badge } from "./badge-B3p-cBAM.js";
import { P as PageHero } from "./PageHero-CQ-1rbtd.js";
import { c as createLucideIcon } from "./createLucideIcon-Dn0WUx8o.js";
import { B as BookOpen, G as GraduationCap } from "./users-WAU5C3w0.js";
import { F as FileText } from "./file-text-DKVFgU-w.js";
import { U as UserPlus } from "./user-plus-C3OLx08z.js";
import { C as CircleCheck } from "./circle-check-CLUGLzcT.js";
import { C as CirclePlay } from "./circle-play-C_7qxHed.js";
import "node:async_hooks";
import "node:stream/web";
import "node:stream";
import "./types-Ch40mmLW.js";
import "./index-ChW4vIqc.js";
import "./button-DInpa_86.js";
import "./Logo-wkBcYT7E.js";
import "./index-BHnLLcIP.js";
import "./sparkles-DBEmDCt9.js";
const __iconNode$8 = [
  ["path", { d: "M12 21V7", key: "gj6g52" }],
  ["path", { d: "m16 12 2 2 4-4", key: "mdajum" }],
  [
    "path",
    {
      d: "M22 6V4a1 1 0 0 0-1-1h-5a4 4 0 0 0-4 4 4 4 0 0 0-4-4H3a1 1 0 0 0-1 1v13a1 1 0 0 0 1 1h6a3 3 0 0 1 3 3 3 3 0 0 1 3-3h6a1 1 0 0 0 1-1v-1.3",
      key: "8arnkb"
    }
  ]
];
const BookOpenCheck = createLucideIcon("book-open-check", __iconNode$8);
const __iconNode$7 = [
  ["path", { d: "M16 19h6", key: "xwg31i" }],
  ["path", { d: "M16 2v4", key: "4m81vk" }],
  ["path", { d: "M19 16v6", key: "tddt3s" }],
  ["path", { d: "M21 12.598V6a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h8.5", key: "1glfrc" }],
  ["path", { d: "M3 10h18", key: "8toen8" }],
  ["path", { d: "M8 2v4", key: "1cmpym" }]
];
const CalendarPlus = createLucideIcon("calendar-plus", __iconNode$7);
const __iconNode$6 = [
  ["rect", { width: "8", height: "4", x: "8", y: "2", rx: "1", ry: "1", key: "tgr4d6" }],
  [
    "path",
    {
      d: "M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2",
      key: "116196"
    }
  ],
  ["path", { d: "M12 11h4", key: "1jrz19" }],
  ["path", { d: "M12 16h4", key: "n85exb" }],
  ["path", { d: "M8 11h.01", key: "1dfujw" }],
  ["path", { d: "M8 16h.01", key: "18s6g9" }]
];
const ClipboardList = createLucideIcon("clipboard-list", __iconNode$6);
const __iconNode$5 = [
  [
    "path",
    {
      d: "M12 22h6a2 2 0 0 0 2-2V8a2.4 2.4 0 0 0-.706-1.706l-3.588-3.588A2.4 2.4 0 0 0 14 2H6a2 2 0 0 0-2 2v6",
      key: "15usau"
    }
  ],
  ["path", { d: "M14 2v5a1 1 0 0 0 1 1h5", key: "wfsgrz" }],
  ["path", { d: "M3 16v-1.5a.5.5 0 0 1 .5-.5h7a.5.5 0 0 1 .5.5V16", key: "s1gz5" }],
  ["path", { d: "M6 22h2", key: "194x9m" }],
  ["path", { d: "M7 14v8", key: "11ixej" }]
];
const FileTypeCorner = createLucideIcon("file-type-corner", __iconNode$5);
const __iconNode$4 = [
  [
    "path",
    {
      d: "M12 3q1 4 4 6.5t3 5.5a1 1 0 0 1-14 0 5 5 0 0 1 1-3 1 1 0 0 0 5 0c0-2-1.5-3-1.5-5q0-2 2.5-4",
      key: "1slcih"
    }
  ]
];
const Flame = createLucideIcon("flame", __iconNode$4);
const __iconNode$3 = [
  ["path", { d: "M12 10v6", key: "1bos4e" }],
  ["path", { d: "M9 13h6", key: "1uhe8q" }],
  [
    "path",
    {
      d: "M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z",
      key: "1kt360"
    }
  ]
];
const FolderPlus = createLucideIcon("folder-plus", __iconNode$3);
const __iconNode$2 = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", ry: "2", key: "1m3agn" }],
  ["circle", { cx: "9", cy: "9", r: "2", key: "af1f0g" }],
  ["path", { d: "m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21", key: "1xmnt7" }]
];
const Image = createLucideIcon("image", __iconNode$2);
const __iconNode$1 = [
  ["path", { d: "M9 18V5l12-2v13", key: "1jmyc2" }],
  ["circle", { cx: "6", cy: "18", r: "3", key: "fqmcym" }],
  ["circle", { cx: "18", cy: "16", r: "3", key: "1hluhg" }]
];
const Music = createLucideIcon("music", __iconNode$1);
const __iconNode = [
  ["path", { d: "M10 14.66v1.626a2 2 0 0 1-.976 1.696A5 5 0 0 0 7 21.978", key: "1n3hpd" }],
  ["path", { d: "M14 14.66v1.626a2 2 0 0 0 .976 1.696A5 5 0 0 1 17 21.978", key: "rfe1zi" }],
  ["path", { d: "M18 9h1.5a1 1 0 0 0 0-5H18", key: "7xy6bh" }],
  ["path", { d: "M4 22h16", key: "57wxv0" }],
  ["path", { d: "M6 9a6 6 0 0 0 12 0V3a1 1 0 0 0-1-1H7a1 1 0 0 0-1 1z", key: "1mhfuq" }],
  ["path", { d: "M6 9H4.5a1 1 0 0 1 0-5H6", key: "tex48p" }]
];
const Trophy = createLucideIcon("trophy", __iconNode);
const heroSummary = "/assets/hero-summary-BUQEvZcG.jpg";
const BADGE_META = {
  first_lesson: { label: "First Lesson", icon: BookOpenCheck, desc: "Completed your first lesson" },
  first_quiz: { label: "First Quiz Passed", icon: ClipboardCheck, desc: "Passed your first quiz" },
  first_course: { label: "First Course", icon: Trophy, desc: "Completed an entire course" },
  streak_7: { label: "7-Day Streak", icon: Flame, desc: "Learned 7 days in a row" }
};
function BadgesPanel({ learnerId }) {
  const { user } = useAuth();
  const id = learnerId || user?.id;
  const [codes, setCodes] = reactExports.useState([]);
  const [loading, setLoading] = reactExports.useState(true);
  reactExports.useEffect(() => {
    if (!id) return;
    (async () => {
      setLoading(true);
      const { data } = await supabase.from("badges").select("code, awarded_at").eq("learner_id", id).order("awarded_at");
      setCodes((data ?? []).map((b) => b.code));
      setLoading(false);
    })();
  }, [id]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Card, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(CardHeader, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(CardTitle, { className: "flex items-center gap-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Award, { className: "h-5 w-5" }),
      " Badges"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(CardContent, { children: loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground", children: "Loading…" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-2 gap-3 sm:grid-cols-4", children: Object.entries(BADGE_META).map(([code, meta]) => {
      const earned = codes.includes(code);
      const Icon = meta.icon;
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `rounded-lg border p-3 text-center transition ${earned ? "bg-primary/10 border-primary" : "opacity-50"}`, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: `mx-auto mb-2 h-7 w-7 ${earned ? "text-primary" : "text-muted-foreground"}` }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold", children: meta.label }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: meta.desc })
      ] }, code);
    }) }) })
  ] });
}
function UserSummaryPage() {
  const {
    user,
    role,
    loading
  } = useAuth();
  const nav = useNavigate();
  const [activity, setActivity] = reactExports.useState([]);
  const [busy, setBusy] = reactExports.useState(true);
  reactExports.useEffect(() => {
    if (!loading && !user) nav({
      to: "/login"
    });
  }, [loading, user, nav]);
  reactExports.useEffect(() => {
    const load = async () => {
      if (!user || !role) return;
      setBusy(true);
      const items = [];
      if (role === "admin") {
        const [{
          data: profiles
        }, {
          data: roles
        }, {
          data: courses
        }, {
          data: lessons
        }, {
          data: enrolls
        }, {
          data: attempts
        }, {
          data: completions
        }, {
          data: vClasses
        }, {
          data: vAttend
        }] = await Promise.all([supabase.from("profiles").select("id, full_name, email"), supabase.from("user_roles").select("user_id, role"), supabase.from("courses").select("id, title, teacher_id, created_at, is_active").order("created_at", {
          ascending: false
        }).limit(100), supabase.from("lessons").select("id, title, content_type, module_id, created_at, modules(course_id, courses(title, teacher_id))").order("created_at", {
          ascending: false
        }).limit(100), supabase.from("enrollments").select("id, learner_id, course_id, enrolled_at, courses(title)").order("enrolled_at", {
          ascending: false
        }).limit(100), supabase.from("quiz_attempts").select("id, learner_id, quiz_id, score, max_score, started_at, submitted_at, quizzes(title)").order("started_at", {
          ascending: false
        }).limit(100), supabase.from("lesson_completions").select("id, learner_id, lesson_id, completed_at, lessons(title)").order("completed_at", {
          ascending: false
        }).limit(100), supabase.from("virtual_classes").select("id, title, teacher_id, scheduled_at, created_at, courses(title)").order("created_at", {
          ascending: false
        }).limit(100), supabase.from("virtual_class_attendance").select("id, learner_id, class_id, joined_at, virtual_classes(title)").order("joined_at", {
          ascending: false
        }).limit(100)]);
        const nameMap = /* @__PURE__ */ new Map();
        (profiles ?? []).forEach((p) => nameMap.set(p.id, p.full_name || p.email || "Unknown user"));
        const roleMap = /* @__PURE__ */ new Map();
        (roles ?? []).forEach((r) => {
          const cur = roleMap.get(r.user_id);
          if (!cur || r.role === "admin" || r.role === "teacher" && cur === "learner") roleMap.set(r.user_id, r.role);
        });
        const who = (uid) => ({
          actor: nameMap.get(uid) ?? "Unknown",
          actorRole: roleMap.get(uid)
        });
        for (const c of courses ?? []) {
          const w = who(c.teacher_id);
          items.push({
            id: `acourse-${c.id}`,
            ts: c.created_at,
            icon: GraduationCap,
            label: `Created course "${c.title}"`,
            badge: c.is_active ? "Active" : "Draft",
            href: `/courses/${c.id}`,
            ...w
          });
        }
        for (const l of lessons ?? []) {
          const teacherId = l.modules?.courses?.teacher_id;
          const w = teacherId ? who(teacherId) : {};
          const icon = l.content_type === "video" ? Video : l.content_type === "audio" ? Music : l.content_type === "pdf" ? FileText : l.content_type === "doc" ? FileTypeCorner : FileText;
          items.push({
            id: `alesson-${l.id}`,
            ts: l.created_at,
            icon,
            label: `Added ${l.content_type} lesson "${l.title}"`,
            detail: l.modules?.courses?.title ? `in ${l.modules.courses.title}` : void 0,
            ...w
          });
        }
        for (const vc of vClasses ?? []) {
          const w = who(vc.teacher_id);
          items.push({
            id: `avc-${vc.id}`,
            ts: vc.created_at,
            icon: CalendarPlus,
            label: `Scheduled virtual class "${vc.title}"`,
            detail: vc.courses?.title ? `for ${vc.courses.title} · ${new Date(vc.scheduled_at).toLocaleString()}` : new Date(vc.scheduled_at).toLocaleString(),
            ...w
          });
        }
        for (const e of enrolls ?? []) {
          const w = who(e.learner_id);
          items.push({
            id: `aenroll-${e.id}`,
            ts: e.enrolled_at,
            icon: UserPlus,
            label: `Enrolled in "${e.courses?.title ?? "a course"}"`,
            href: `/courses/${e.course_id}`,
            ...w
          });
        }
        for (const a of attempts ?? []) {
          const w = who(a.learner_id);
          items.push({
            id: `aattempt-${a.id}`,
            ts: a.submitted_at ?? a.started_at,
            icon: CircleCheck,
            label: a.submitted_at ? `Completed quiz "${a.quizzes?.title ?? ""}"` : `Started quiz "${a.quizzes?.title ?? ""}"`,
            detail: a.submitted_at ? `Score: ${a.score} / ${a.max_score}` : void 0,
            ...w
          });
        }
        for (const lc of completions ?? []) {
          const w = who(lc.learner_id);
          items.push({
            id: `acomp-${lc.id}`,
            ts: lc.completed_at,
            icon: CircleCheck,
            label: `Completed lesson "${lc.lessons?.title ?? ""}"`,
            ...w
          });
        }
        for (const at of vAttend ?? []) {
          const w = who(at.learner_id);
          items.push({
            id: `aattend-${at.id}`,
            ts: at.joined_at,
            icon: CirclePlay,
            label: `Joined virtual class "${at.virtual_classes?.title ?? ""}"`,
            ...w
          });
        }
      } else if (role === "teacher") {
        const {
          data: courses
        } = await supabase.from("courses").select("id,title,subject,class_level,thumbnail_url,is_active,created_at").eq("teacher_id", user.id).order("created_at", {
          ascending: false
        });
        const courseIds = (courses ?? []).map((c) => c.id);
        const courseMap = new Map((courses ?? []).map((c) => [c.id, c]));
        for (const c of courses ?? []) {
          items.push({
            id: `course-${c.id}`,
            ts: c.created_at,
            icon: GraduationCap,
            label: `Created course "${c.title}"`,
            detail: [c.subject, c.class_level].filter(Boolean).join(" · ") || void 0,
            badge: c.is_active ? "Active" : "Draft",
            href: `/courses/${c.id}`
          });
          if (c.thumbnail_url) {
            items.push({
              id: `thumb-${c.id}`,
              ts: c.created_at,
              icon: Image,
              label: `Added a thumbnail to "${c.title}"`,
              href: `/courses/${c.id}`
            });
          }
        }
        if (courseIds.length) {
          const {
            data: modules
          } = await supabase.from("modules").select("id,course_id,title,created_at").in("course_id", courseIds).order("created_at", {
            ascending: false
          });
          const modIds = (modules ?? []).map((m) => m.id);
          const modCourse = new Map((modules ?? []).map((m) => [m.id, m.course_id]));
          for (const m of modules ?? []) {
            const c = courseMap.get(m.course_id);
            items.push({
              id: `mod-${m.id}`,
              ts: m.created_at,
              icon: FolderPlus,
              label: `Added module "${m.title}"`,
              detail: c ? `in ${c.title}` : void 0,
              href: c ? `/courses/${c.id}` : void 0
            });
          }
          if (modIds.length) {
            const {
              data: lessons
            } = await supabase.from("lessons").select("id,module_id,title,content_type,created_at").in("module_id", modIds).order("created_at", {
              ascending: false
            });
            for (const l of lessons ?? []) {
              const courseId = modCourse.get(l.module_id);
              const c = courseId ? courseMap.get(courseId) : void 0;
              const icon = l.content_type === "video" ? Video : l.content_type === "audio" ? Music : l.content_type === "pdf" ? FileText : l.content_type === "doc" ? FileTypeCorner : FileText;
              const verb = l.content_type === "text" ? "Added text lesson" : `Uploaded ${l.content_type}`;
              items.push({
                id: `lesson-${l.id}`,
                ts: l.created_at,
                icon,
                label: `${verb} "${l.title}"`,
                detail: c ? `in ${c.title}` : void 0,
                href: c ? `/courses/${c.id}` : void 0
              });
            }
            const {
              data: quizzes
            } = await supabase.from("quizzes").select("id,course_id,title,created_at").in("course_id", courseIds).order("created_at", {
              ascending: false
            });
            for (const q of quizzes ?? []) {
              const c = courseMap.get(q.course_id);
              items.push({
                id: `quiz-${q.id}`,
                ts: q.created_at,
                icon: ClipboardList,
                label: `Created quiz "${q.title}"`,
                detail: c ? `in ${c.title}` : void 0,
                href: c ? `/courses/${c.id}` : void 0
              });
            }
          }
        }
      } else if (role === "learner") {
        const {
          data: enrolls
        } = await supabase.from("enrollments").select("id,enrolled_at,course_id,courses(title)").eq("learner_id", user.id).order("enrolled_at", {
          ascending: false
        });
        for (const e of enrolls ?? []) {
          items.push({
            id: `enroll-${e.id}`,
            ts: e.enrolled_at,
            icon: UserPlus,
            label: `Enrolled in "${e.courses?.title ?? "a course"}"`,
            href: `/courses/${e.course_id}`
          });
        }
        const {
          data: attempts
        } = await supabase.from("quiz_attempts").select("id,started_at,submitted_at,score,max_score").eq("learner_id", user.id).order("started_at", {
          ascending: false
        });
        for (const a of attempts ?? []) {
          items.push({
            id: `attempt-${a.id}`,
            ts: a.submitted_at ?? a.started_at,
            icon: CircleCheck,
            label: a.submitted_at ? `Completed a quiz` : `Started a quiz`,
            detail: a.submitted_at ? `Score: ${a.score} / ${a.max_score}` : void 0
          });
        }
      }
      items.sort((a, b) => new Date(b.ts).getTime() - new Date(a.ts).getTime());
      setActivity(items);
      setBusy(false);
    };
    load();
  }, [user, role]);
  if (loading || !user) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex min-h-screen items-center justify-center text-muted-foreground", children: "Loading…" });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SidebarProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex min-h-screen w-full", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(AppSidebar, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-1 flex-col", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "flex h-14 items-center gap-2 border-b bg-card px-4", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(SidebarTrigger, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-medium text-muted-foreground", children: "User Summary" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ml-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx(NotificationBell, {}) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("main", { className: "flex-1 space-y-8 p-6 md:p-8", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(PageHero, { eyebrow: "Activity timeline", EyebrowIcon: ClipboardList, title: "Activity", description: role === "admin" ? "Platform-wide timeline of actions taken by every learner, teacher, and admin." : role === "teacher" ? "A timeline of every course, module, lesson, quiz, and material you've added." : "Your enrollments and quiz activity.", backgroundImage: heroSummary }),
        role === "learner" && /* @__PURE__ */ jsxRuntimeExports.jsx(BadgesPanel, { learnerId: user.id }),
        busy ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: "Loading activity…" }) : activity.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { className: "border-dashed", style: {
          background: "var(--gradient-emerald-soft)"
        }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(CardContent, { className: "flex flex-col items-center gap-3 py-16 text-center", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex h-14 w-14 items-center justify-center rounded-full bg-primary/10 text-primary", children: /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { className: "h-7 w-7" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-base font-medium", children: "No activity yet" })
        ] }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("ol", { className: "space-y-3", children: activity.map((a) => {
          const Inner = /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { className: a.href ? "border-border/60 transition-all hover:-translate-y-0.5 hover:border-primary/40" : "border-border/60", style: {
            boxShadow: "var(--shadow-card)"
          }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(CardContent, { className: "flex items-start gap-4 p-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex h-10 w-10 flex-none items-center justify-center rounded-full bg-primary/10 text-primary", children: /* @__PURE__ */ jsxRuntimeExports.jsx(a.icon, { className: "h-5 w-5" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap items-center gap-2", children: [
                a.actor && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-semibold", children: [
                  a.actor,
                  a.actorRole && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: a.actorRole === "admin" ? "destructive" : a.actorRole === "teacher" ? "default" : "secondary", className: "ml-2 align-middle", children: a.actorRole })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-medium", children: a.label }),
                a.badge && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "secondary", children: a.badge })
              ] }),
              a.detail && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground", children: a.detail })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("time", { className: "hidden flex-none text-sm text-muted-foreground sm:block", children: new Date(a.ts).toLocaleString() })
          ] }) });
          return /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: a.href ? /* @__PURE__ */ jsxRuntimeExports.jsx(Link, { to: a.href, children: Inner }) : Inner }, a.id);
        }) })
      ] })
    ] })
  ] }) });
}
export {
  UserSummaryPage as component
};
